---
features:
  - |
    Adds `atol` parameter to `~.SecondQuantizedOp.is_hermitian`
fixes:
  - |
    Fixes a bug that caused `~.FermionicOp.is_hermitian` to return false negatives
    due to not normal-ordering.
