---
prelude: >
    For qiskit-nature issues #91

features:
  - |
    Adds succ_full ansatz option to ``SUCCD`` objects. It can be used in the ``SUCCD`` function,
    for example::

      from qiskit_nature.circuit.library.ansatzes import SUCCfull
      from qiskit_nature.converters.second_quantization import QubitConverter
      from qiskit_nature.mappers.second_quantization import JordanWignerMapper
      converter = QubitConverter(JordanWignerMapper())
      ansatz = SUCCD(converter, (1, 1), 6, full=True)

issues:
  - |
